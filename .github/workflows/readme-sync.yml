name: README Sync

on:
  push:
  pull_request:

jobs:
  readme-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate README deploy usage sync
        run: ./scripts/check-readme-sync.sh
